steps:
  # name: gcr.io/$PROJECT_ID/protoc:latest
  # args:
  #   - -I:authed
  #   - authed/authed.proto
  #   - --js_out=import_style=commonjs:authed
  #   - --grpc-web_out=import_style=commonjs,mode=grpcwebtext:authed
  #   - --go_out=plugins=grpc:authed
  - name: gcr.io/cloud-builders/docker:latest
    args:
      - build
      - --cache-from
      - gcr.io/$PROJECT_ID/$_IMG:latest
      - -t
      - gcr.io/$PROJECT_ID/$_IMG:latest
      - -t
      - gcr.io/$PROJECT_ID/$_IMG:$SHORT_SHA
      - .
  - name: gcr.io/cloud-builders/kubectl:latest
    args:
      - set
      - image
      - deploy
      - $_IMG
      - "*=gcr.io/$PROJECT_ID/$_IMG:$SHORT_SHA"
    env:
      - "CLOUDSDK_COMPUTE_ZONE=us-central1-a"
      - "CLOUDSDK_CONTAINER_CLUSTER=cluster12"
images:
  - gcr.io/$PROJECT_ID/$_IMG:latest
  - gcr.io/$PROJECT_ID/$_IMG:$SHORT_SHA
substitutions:
  _IMG: authed
